# ==============================================================================
# File: crates/geist-automation/Cargo.toml
# Layer: automation
# Purpose: Automation lanes + modulation matrix unified into parameter values.
# Status: Implemented incrementally; curve shapes first.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

[package]
name = "geist-automation"
version = "0.1.0"
edition = "2021"

[dependencies]
geist-core = { path = "../geist-core" }
