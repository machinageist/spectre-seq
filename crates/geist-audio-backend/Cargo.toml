# ==============================================================================
# File: crates/geist-audio-backend/Cargo.toml
# Layer: audio I/O
# Purpose: Platform audio I/O abstraction plus the cpal backend implementation.
# Status: Implemented; backend abstraction + cpal output backend.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

[package]
name = "geist-audio-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
geist-core = { path = "../geist-core" }
# Cross-platform audio I/O; wraps CoreAudio/WASAPI/ALSA behind one API
cpal = "0.15"
