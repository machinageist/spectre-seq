# ==============================================================================
# File: crates/geist-config/Cargo.toml
# Layer: config
# Purpose: User and project workflow configuration.
# Status: Phase 8 scaffold with validated schema.
# Contract: Config is declarative and never runs on the audio callback.
# ==============================================================================

[package]
name = "geist-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
