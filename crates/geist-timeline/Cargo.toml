# ==============================================================================
# File: crates/geist-timeline/Cargo.toml
# Layer: timeline
# Purpose: Arrangement, clips, transport, and undo/redo; app-thread timeline model.
# Status: Implemented incrementally; arena foundation first.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

[package]
name = "geist-timeline"
version = "0.1.0"
edition = "2021"

[dependencies]
geist-core = { path = "../geist-core" }
# Compatibility window; the prelude re-exports relocated document types
geist-document = { path = "../geist-document" }
