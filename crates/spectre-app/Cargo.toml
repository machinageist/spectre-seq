# Author: Jeff
# Date: 2026-07-12
# Description: Launchable Spectre interaction prototype
# Notes: Native egui shell; audio engine and render graph remain intentionally absent

[package]
name = "spectre-app"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
eframe = { version = "0.32.3", default-features = false, features = ["default_fonts", "glow"] }
spectre-core = { path = "../spectre-core" }
spectre-dsp = { path = "../spectre-dsp" }
spectre-project = { path = "../spectre-project" }
serde.workspace = true
serde_json.workspace = true
