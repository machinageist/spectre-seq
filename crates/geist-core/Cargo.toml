# ==============================================================================
# File: crates/geist-core/Cargo.toml
# Layer: core primitives
# Purpose: Define Cargo responsibilities and integration boundaries.
# Status: Pseudocode scaffold; implementation intentionally pending.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

# Pseudocode plan:
# - Declare responsibility: Define Cargo responsibilities and integration boundaries.
# - Define public types before behavior.
# - Separate real-time-safe paths from UI/app paths.
# - Prefer explicit errors over implicit panics.
# - Add tests beside behavior once implementation begins.
# - Keep crate dependency-free inside workspace.
# - Use newtypes for IDs; avoid primitive leakage.
# - Expose minimal prelude through lib.rs.
# - Keep config explicit and reproducible.
# - Document each non-default setting inline.

[package]
name = "geist-core"
version = "0.1.0"
edition = "2021"

[dependencies]
