# ==============================================================================
# File: crates/spectre-document/Cargo.toml
# Layer: document
# Purpose: Canonical app-thread project document; durable identity and arrangement.
# Status: Implemented incrementally; identity and arrangement relocated first.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

[package]
name = "spectre-document"
version = "0.1.0"
edition = "2021"

[dependencies]
spectre-core = { path = "../spectre-core" }
