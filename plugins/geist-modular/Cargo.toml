# ==============================================================================
# File: plugins/geist-modular/Cargo.toml
# Layer: modular utilities
# Purpose: Utility/glue nodes; pure routing math wrapped as graph AudioNodes.
# Status: Implemented incrementally; math nodes first.
# Contract: Keep comments terse, declarative, and synchronized with code.
# ==============================================================================

[package]
name = "geist-modular"
version = "0.1.0"
edition = "2021"

[dependencies]
geist-core = { path = "../../crates/geist-core" }
geist-graph = { path = "../../crates/geist-graph" }
