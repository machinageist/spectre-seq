# ==============================================================================
# File: rust-toolchain.toml
# Layer: toolchain pin
# Purpose: pin the stable Rust release channel
# Status: active
# Contract: Nightly features require an accepted decision and measured need.
# ==============================================================================

[toolchain]
channel = "stable"
