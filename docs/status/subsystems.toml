# Author: Jeff
# Date: 2026-07-11
# Description: Machine-readable maturity ledger for the pre-rebuild Geist DAW
# Notes: States are evidence classifications, not feature-completion claims

schema_version = 1
last_verified = "2026-07-11"
repository_head = "1e5314d1b9bb0d27698f468b442158ba9c4952be"
environment = "macOS 27.0 arm64; source audit plus automated baseline"

[[subsystem]]
id = "audio-output-startup"
state = "integrated but not exercised end-to-end"
evidence = ["app/geist-daw/src/init.rs:33-74", "crates/geist-audio-backend/src/cpal_backend.rs:152-197"]
gaps = ["no launch evidence", "default device only", "no reconfiguration path"]

[[subsystem]]
id = "callback-block-bridge"
state = "end-to-end exercised in a controlled fixture"
evidence = ["crates/geist-audio-backend/src/bridge.rs:47-69", "crates/geist-audio-backend/src/bridge.rs:107-153"]
gaps = ["output-only bridge", "no deadline or stress evidence"]

[[subsystem]]
id = "fixed-render-engine"
state = "end-to-end exercised in a controlled fixture"
evidence = ["app/geist-daw/src/init.rs:41-74", "app/geist-daw/src/engine.rs:835-894", "app/geist-daw/src/engine.rs tests"]
gaps = ["not the compiled graph", "no hardware callback run", "fixed track model"]

[[subsystem]]
id = "compiled-render-graph"
state = "unit-tested in isolation"
evidence = ["crates/geist-graph/src/process_list.rs", "crates/geist-graph/src/swap.rs"]
gaps = ["not constructed by application startup", "not the active callback processor"]

[[subsystem]]
id = "native-synth-effects"
state = "end-to-end exercised in a controlled fixture"
evidence = ["app/geist-daw/src/engine.rs:738-772", "crates/geist-synth/src/daw_node.rs", "app/geist-daw/src/fx.rs"]
gaps = ["direct fixed-engine ownership", "not canonical device graph"]

[[subsystem]]
id = "stacksynth"
state = "unit-tested in isolation"
evidence = ["crates/geist-stacksynth/src"]
gaps = ["not an application dependency", "no live processing path"]

[[subsystem]]
id = "modular-rack"
state = "unit-tested in isolation"
evidence = ["crates/geist-modular/src"]
gaps = ["not an application dependency", "UI graph is not audio graph"]

[[subsystem]]
id = "transport-sequencing"
state = "end-to-end exercised in a controlled fixture"
evidence = ["app/geist-daw/src/engine.rs", "crates/geist-timeline/src"]
gaps = ["fixed-engine semantics", "no device-clock or stress evidence"]

[[subsystem]]
id = "external-midi-io"
state = "absent"
evidence = []
gaps = ["no active MIDI backend crate", "no startup ingestion or timestamp path"]

[[subsystem]]
id = "automation"
state = "unit-tested in isolation"
evidence = ["crates/geist-automation/src"]
gaps = ["geist-daw does not depend on geist-automation", "not evaluated in live render"]

[[subsystem]]
id = "synth-modulation"
state = "end-to-end exercised in a controlled fixture"
evidence = ["app/geist-daw/src/engine.rs:738-772", "app/geist-daw/src/engine.rs:1918-1923"]
gaps = ["narrow app-local LFO path", "not general parameter modulation"]

[[subsystem]]
id = "audio-recording"
state = "integrated but not exercised end-to-end"
evidence = ["app/geist-daw/src/init.rs:76-81", "app/geist-daw/src/recorder.rs", "app/geist-daw/src/studio.rs:1011-1015"]
gaps = ["memory until stop", "no latency correction", "no disk streaming", "no crash salvage"]

[[subsystem]]
id = "vst3-host-core"
state = "unit-tested in isolation"
evidence = ["crates/geist-vst-host/src"]
gaps = ["not an application dependency", "no repository real-plugin fixture", "no events or parameter queues"]

[[subsystem]]
id = "vst3-user-workflow"
state = "absent"
evidence = []
gaps = ["no scan invocation", "no instantiation UI", "no editor", "no persisted state or missing-plugin placeholder"]

[[subsystem]]
id = "ui-command-reflection"
state = "integrated but not exercised end-to-end"
evidence = ["app/geist-daw/src/main.rs:68-94", "app/geist-daw/src/control.rs", "app/geist-daw/src/studio.rs"]
gaps = ["no manual UI QA", "duplicated state authority", "accessibility unverified"]

[[subsystem]]
id = "project-manual-save-load"
state = "integrated but not exercised end-to-end"
evidence = ["app/geist-daw/src/session.rs", "app/geist-daw/src/studio.rs:1785-1798"]
gaps = ["demo-shaped mapping", "no cross-version or failure drill"]

[[subsystem]]
id = "project-atomic-write-migration"
state = "unit-tested in isolation"
evidence = ["crates/geist-project/src/autosave.rs", "crates/geist-project/src/migrate.rs", "crates/geist-project/src/serialize.rs"]
gaps = ["no fsync proof", "no disk-full or interruption tests", "no rollback or unknown-field policy"]

[[subsystem]]
id = "autosave-recovery-application"
state = "unit-tested in isolation"
evidence = ["crates/geist-project/src/autosave.rs:75-123"]
gaps = ["application never instantiates Autosaver", "application never scans recovery files", "worker ignores write errors"]

[[subsystem]]
id = "gui-shutdown"
state = "integrated but not exercised end-to-end"
evidence = ["app/geist-daw/src/main.rs:68-94", "crates/geist-audio-backend/src/cpal_backend.rs:258-269"]
gaps = ["implicit RAII only", "no note flush, recording finalization, or teardown test"]

[[subsystem]]
id = "headless-shutdown"
state = "absent"
evidence = ["app/geist-daw/src/main.rs:96-113"]
gaps = ["infinite loop", "no signal handling or orderly shutdown"]
