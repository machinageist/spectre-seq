<!--
Author: Jeff
Date: 2026-07-12
Description: Traceability from observations/rationale through requirements to code and evidence
Notes: One row per requirement that has moved past proposed; grows with each slice
-->

# Traceability

- **Status:** verified
- **Last verified:** 2026-08-28
- **Scope:** requirements with implementation or verification evidence
- **Decision authority:** Jeff
- **Upstream sources:** `requirements-ledger.md`; `../02-reference-research/*observations*.md`
- **Downstream dependents:** milestone exit reviews, release gates
- **Supersedes:** none
- **Superseded by:** none
- **Open decisions:** none
- **Known gaps:** RT-001..003 hold under a live macOS driver as of 2026-08-09. Linux device qualification has never run and is decision-23 debt carried to R4; no Linux support is claimed. GRAPH-002 has only the implicit-cycle seed and did not close at R2 exit. This table is behind the code: R4 slices 1, 2, and 4 landed on 2026-08-24 without rows here, and only slice 6's row below was added at its landing

Chain: provenance → requirement → implementation → evidence (repository-root workspace, 2026-08-09).

| Requirement | Implementation | Evidence | State |
|---|---|---|---|
| TIME-001 (explicit time types) | `crates/spectre-core/src/time.rs` | type-level API (no raw time in public surface); unit tests | implemented |
| TIME-002 (half-open ranges, same-sample order) | `transport.rs` (`LoopRegion`), `event.rs` (rank ordering) | `loop_end_is_exclusive`, `same_sample_off_before_on`, property `event_order_is_permutation_invariant`, `loop_wrap_stays_in_region` | implemented |
| TIME-003 (tempo map conversion) | `tempo.rs` (`TempoMap`) | `tempo_time_003.rs`: 24-hour exact/fractional piecewise oracles, round-once discriminator, signed pre-roll, boundary ownership, monotonicity, exact tick round trip, nearest-tick sample bounds | verified |
| TIME-004 (meter map) | `crates/spectre-core/src/meter.rs` | validated signatures/maps, Serde rejection and round trip, exact bar length, deterministic lookup properties | implemented |
| TIME-005 (transport state machine) | `transport.rs` | 5 unit tests + wrap property; no audio device needed | implemented |
| CORE-001 (stable IDs) | `id.rs` (`ObjectId`, `IdGen`), `spectre_app::{DeviceControl, ParameterControl}`, `spectre_dsp::DeviceParameterSnapshot` | generator uniqueness/determinism; validated transparent numeric decode rejects zero while preserving nonzero JSON round trips; save/load and undo identity; prototype device/parameter nonzero project-instance uniqueness; snapshot identity stability across edits; offline deserialization-boundary and duplicate/alias rejection. Reorder evidence remains gated on R4 and migration evidence on R5 | implemented |
| CORE-002 (parameter descriptors) | `param.rs`, `spectre_dsp::DspParameter` | validation and non-finite containment; exact endpoints, finite monotonic mapping, nearest-`f32` plain quantization, nextafter endpoint behavior, and deterministic seeded normalized round trips within the declared 8,192-ULP fixture ceiling for every native descriptor | implemented |
| CORE-003 (versioned envelope, forward preservation) | `crates/spectre-project/src/lib.rs` | checked-in `tests/fixtures/r1-canonical.json`; byte-stable fixture rewrite, exact round trip, newer-schema rejection, tempo/meter/loop semantic rejection, project/envelope unknown-field preservation | verified |
| R1 beat representation | `crates/spectre-core/src/time.rs` | `beat_ticks_contract.rs`: accepted 960 PPQ, exact common grids, checked positive/negative overflow boundaries, transparent signed-integer JSON round trip | verified |
| R0 offline harness | `crates/spectre-offline/` | deterministic report test, malformed-project test, `cargo run --locked -p spectre-offline -- --self-test` | verified |
| R1 command/undo seed | `crates/spectre-project/src/command.rs` | atomic failure rollback, reversible rename, redo invalidation, bounded-history eviction, identity/unknown-field preservation | implemented |
| Interaction prototype | `crates/spectre-app/`, `./spectre` | 27 app-model tests, including stable deterministic Pulse focus, atomic Build → Shape selection, invalid-ID rollback, lens/edit continuity, descriptor/parameter identity, focus-independent snapshot attribution, nonzero unique project-instance identity, canonical descriptor clamping, exact signed-zero/subnormal and non-finite containment, renderer-neutral presentation, and recoverable UI error reporting; one process smoke test; state-rich selected-device feedback report | prototype |
| DSP device I/O | `docs/03-architecture/dsp-device-io.md`, `crates/spectre-dsp/src/io.rs` | layout, semantic event-order, bounded-capacity, overlap identity, buffer-shape, finite-output, deterministic-source, and sample-offset tests | implemented |
| Native device seed | `crates/spectre-dsp/` | Pulse instrument, ToneSource, Gain, Saturator; twelve device tests, including numeric mapping and boundary policy | implemented |
| CLIP-001..008 (R4-5 clip and schedule bounds) | `crates/spectre-project/src/clip.rs`, `crates/spectre-audio/src/clip.rs`, `RenderBridge::with_clip_player` | 10 model tests (`crates/spectre-project/tests/clip_model.rs`), 14 schedule and player tests (`crates/spectre-audio/tests/clip_schedule.rs`), 13 bridge tests (`crates/spectre-audio/tests/clip_bridge.rs`), 5 app-model tests, 1 smoke test. `clip_and_live_notes_merge_into_one_sorted_block` drives both producers for 200 blocks at zero plan errors; `the_live_bridge_hash_matches_the_offline_render` pins live/offline equivalence over one event list. Both were falsified before being trusted | implemented; ledger rows `proposed` pending Jeff's acceptance |
| DEV-001..013 (R4-6 native device bounds) | `crates/spectre-dsp/src/filament.rs`, `gloam.rs`; `spectre_offline::render_voice_chain` | 30 device tests in `tests/devices.rs` and 3 in `tests/device_rt.rs`; `filament_and_gloam_report_no_containment_activity`; `voice_chain_renders_deterministically` and `..._exact_silence_without_events`; `voice_chain_process_is_rt_clean_through_the_null_stream`, falsified by a deliberate allocation before being trusted | implemented; ledger rows `proposed` pending Jeff's acceptance |
| Native render fixture | `spectre_offline::render_vertical_slice` via `spectre-graph` plan | repeated render equality; bit-identical to a hand-wired chain; exact-silence gate; impulse sample-exactness; allocation-free steady-state quanta (counting allocator); FNV hash determinism | verified |
| Backend-derived device UI | `spectre_app::{DeviceControl, AppModel::open_device_in_shape}`, Build and Shape lenses, `spectre_dsp::DeviceParameterSnapshot` | one labeled Build action per existing device; selected-only Shape controls retain descriptor labels, units, ranges, defaults, reset/setter behavior, and stable device/parameter `ObjectId`s; fail-closed focus transition; renderer-neutral private-field DTO with instance/key getters and canonical clamping; app export stays selection-independent and preserves edited-value attribution and exact signed-zero/subnormal bits | prototype (offline-integrated) |
| Runtime parameter seam (decision 22) | `docs/03-architecture/dsp-device-io.md` § "Runtime parameter seam" | accepted R3 design review: callback-safe single-value application keyed by static parameter key, app-thread validation and clamping against existing descriptors, fail-closed unknown keys, block-boundary application before `process`, device-owned smoothing; implementation lands R4, sample-accurate automation stays PROD-002 at R9 | accepted (design) |
| MIDI timestamp ingress | `spectre_audio::midi`; `NoteEventKind::rank` made public in `spectre-dsp` | `midi_ingress.rs`: absolute timestamps to block-relative offsets, after-block refusal and later-block delivery, late-message clamping with a counter, releases before attacks at one offset, arrival order preserved at equal rank, out-of-order timestamps sorted into contract order, note-off carrying its note-on's allocated nonzero ID, zero-velocity note-on treated as release, unmatched release refused, all-notes-off releasing the channel, unsupported and out-of-range refusal, buffer-full refusal, capacity clamped to the 1,024-event contract limit, and ingress output accepted by the real plan's ordering validation | implemented |
| Health telemetry | `spectre_audio::bridge::BridgeTelemetry` | `lifecycle_health.rs`: headroom published per block and finite only after a render, worst-case headroom starting at infinity and never exceeding the latest block, an overrunning block counted as an xrun with non-positive headroom, and containment counters reaching the app thread | implemented |
| Device lifecycle drill | `lifecycle_health.rs` | Deterministic half passes on the null backend: three start/stop/restart cycles preserving render counts across gaps, sample-rate changes reopening without losing the device, device loss reported as `Closed` on every operation with recovery on a fresh stream. Hardware half passed on macOS 2026-08-09 (cpal/CoreAudio, M-Audio AIR 192\|6): two start/stop cycles on a live stream, 173 driver callbacks, 0 xruns, 0 plan errors, 0 contaminated nodes, worst-case headroom 0.990. Linux device qualification has not run. Linux build/portability verified 2026-08-09 in an aarch64 container (workspace links against ALSA, 34/34 non-hardware audio tests pass), and the drill was confirmed to fail closed with no device rather than report a false pass | partial — macOS qualified, Linux device outstanding |
| CORE-004 (atomic-save API design) | `docs/03-architecture/project-persistence.md` | accepted R1 design review: boundaries, ordered save algorithm, `SaveReceipt`/`SaveError`/`TargetState` vocabulary, failure-stage guarantees, deterministic fault-injection seam; filesystem implementation lands R4, crash qualification R5 | accepted (design) |
| GRAPH-001 (plan/graph type seam) | `crates/spectre-graph/src/lib.rs`, `docs/03-architecture/graph-compilation.md`, `spectre_offline::render_app_snapshot` | `graph_plan.rs`: deterministic repeated render, edit/compile validation, event-routing refusals, frame bounds, unreachable-node exclusion; plan exposes no mutation API; targeted app/offline tests prove complete validated app snapshots select processor values during immutable-plan construction and deterministically affect render reports | implemented |
| GRAPH-002 (explicit priced feedback) | implicit-cycle rejection only (`GraphError::Cycle` diagnostic) | cycle fixture in `graph_plan.rs`; explicit one-quantum-delay feedback edges not designed or implemented | proposed |
| Audio backend seam | `crates/spectre-audio/` (`AudioBackend`, `AudioStream`, `NullBackend`, `CpalBackend`) | 15 tests: null enumeration and default reporting, inclusive `StreamConfig` bound acceptance and out-of-range rejection, unknown-device and invalid-config rejection before any driver call, full Stopped→Running→Stopped→Closed walk, fail-closed invalid transitions including use-after-close, exact block geometry and silence fill, callback driven off the enumerating thread, allocation-free steady-state pump, and cpal enumeration that is well-formed with or without hardware | implemented (decision 19) |
| RT-002 (bounded wait-free control↔render) | `spectre_audio::spsc` (SPSC ring), `spectre_audio::control` (split lanes, reclaim path) | 19 tests: 5,000-write sweep coalescing to one application, changed-only drain, bit-exact signed-zero/subnormal/NaN delivery, unknown/out-of-range/duplicate target rejection, strict-FIFO notes and transport, counted overflow that hands the value back rather than dropping it, retired state dropped on the app thread and never the render thread, cross-thread FIFO over 100,000 items, repeated mask wrap without corruption, teardown dropping elements across the wrap, and allocation-free send/drain | implemented (decision 21) |
| Callback bridge over the compiled plan | `spectre_audio::bridge::RenderBridge` | `bridge_plan.rs`: bridge output hashes identically to `render_vertical_slice` on the same fixture, events, and device values, with a nonzero peak proving the match is not two silent buffers; oversized blocks refused into exact silence; transport applied in order; notes beyond the scratch deferred and later delivered without loss; repeated renders deterministic. `pump_alloc.rs` adds an allocation-free steady-state render | implemented (GRAPH-001 reuse; no second render path) |
| RT-001 (no allocation, locks, I/O, logging, or panics across the callback boundary) | `spectre_audio::{bridge, control, spsc, null}`; `AudioProcessor: Send` in `spectre-dsp` | `rt_guard.rs`: RT-section allocator guard attributing violations to the exact call, with a positive control proving the guard fires; zero violations across bridge render, frame-capacity and empty-block refusals, control drain, retire, and the plan driven through a real backend callback; structural scan proving the RT modules name no blocking primitive; compile-time `Send` assertions on `CompiledPlan`, `RenderBridge`, and both control halves. `pump_alloc.rs` retains the steady-state count gates | implemented for the offline-driven callback path; a live driver is still unqualified |
| BOUNCE-001..003 (R4-8 offline bounce bounds) | `crates/spectre-offline/src/{hash,fixture,wav,bounce}.rs`; `--bounce` in `main.rs`; `crates/spectre-app/src/bounce_panel.rs` | 15 unit tests (`crates/spectre-offline/tests/bounce.rs`), 5 CLI round-trip tests (`bounce_cli.rs`), 3 equivalence tests (`crates/spectre-audio/tests/bounce_equivalence.rs`), 8 panel tests (`crates/spectre-app/tests/bounce_panel.rs`), 1 containment test. `a_multi_block_bounce_matches_the_live_path_block_for_block` is the central gate; `a_mid_timeline_note_lands_on_the_same_frame_in_both_paths` is the rebase gate and catches two mutations the first cannot; `the_shared_fold_matches_its_checked_in_golden_vector` pins the fold against a constant computed outside the workspace. Bit-exactness of the `fixture.rs`/`hash.rs` extraction is proven by `harness.rs` (24/24) and `bridge_plan.rs` (6/6) passing with no assertion changed | implemented; ledger rows `proposed` pending Jeff's acceptance |
| RT-003 (denormal flush, NaN/Inf containment) | `spectre_graph::contain_channel` in `CompiledPlan::process`; `ContainmentStats`; republished by `BridgeTelemetry` | `containment.rs`: NaN, +Inf, and -Inf each silence the node to exact zero and name it; denormals flush to zero with sign preserved and are counted as flushes rather than contamination; clean output untouched and uncounted; a contaminated node is isolated so a downstream `Gain` receives finite silence; one poisoned sample silences the whole node; counters accumulate across quanta; the four shipping devices trip nothing during ordinary rendering | accepted and implemented 2026-08-09 |

Full-gate result (2026-08-09, repository root, rustc/cargo 1.97.1, after the Geist → Spectre rename and R3 slices 2-9): formatting clean; strict Clippy clean; 230/230 tests pass with one ignored hardware drill, of which 75 are the new backend seam, control transport, callback bridge, RT-001 guards, RT-003 containment, MIDI ingress, and lifecycle/health evidence; `./spectre --smoke-test` identifies Pulse selection and the offline self-test passes. The interaction evidence includes 27/27 app-model tests and 1/1 process smoke test; the offline harness remains 21/21. R2 silence/impulse/allocation/hash gates remain green on the unchanged compiled-plan path.
